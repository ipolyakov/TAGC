[project]
version = "0.1.0"
name = "tagc"

[tool.setuptools.packages.find]
include = ["tagc"]

[tool.isort]
profile = "black"
py_version = "310"
line_length = 120

[tool.black]
line-length = 120
target-version = ["py310"]
required-version = "23.7.0"
